// Notification utility functions
export function sendNotification(message: string, type: 'success' | 'error') {
  // Example for a simple notification system (e.g., using a toast library)
  console.log(`${type.toUpperCase()}: ${message}`);
}
